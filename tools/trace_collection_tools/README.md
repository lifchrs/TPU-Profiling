`process_traces.py` is a script that processes trace files and extracts communication kernel information, saving  it to a parquet file. 

After running this, you can use `generate_plots.py` to generate plots from the parquet file.

